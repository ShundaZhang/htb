from pwn import *

ip, port = '157.245.32.12', 31915
io = remote(ip, port)

context.log_level = 'debug'

io.recvuntil('3 seconds!')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
buf = io.recvline()
print buf.encode('hex')
